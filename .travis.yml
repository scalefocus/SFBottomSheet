language: objective-c
osx_image: xcode12.2
xcode_project: SFBottomSheet.xcodeproj
xcode_scheme: SFBottomSheetDemo
xcode_sdk: iphonesimulator14.2
# env:
#   matrix:
#     - TEST_SDK=iphonesimulator12.1 OS=13.3 name='iPhone XR'
#     - TEST_SDK=iphonesimulator12.1 OS=12.1 name='iPhone 8'

# script:
#   - set -o pipefail && xcodebuild test -enableCodeCoverage YES -project SFBottomSheet.xcodeproj -scheme SFBottomSheet -sdk $TEST_SDK -destination "platform=iOS Simulator,OS=$OS,name=$NAME" ONLY_ACTIVE_ARCH=YES
# after_success:
#   - slather
#   - bash <(curl -s https://codecov.io/bash) -f reports/cobertura.xml -X coveragepy -X gcov -X xcode
